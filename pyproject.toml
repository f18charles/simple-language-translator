[project]
name = "translator"
version = "0.1.0"
description = "A simple demo translator using predefined phrases"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.4",
]
